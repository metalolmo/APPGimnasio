<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Estilo personalizado para la fecha -->
    <style name="CalendarDateTextWhite">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">14sp</item>
    </style>

    <!-- Estilo personalizado para los días de la semana -->
    <style name="CalendarWeekDayTextWhite">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">12sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- Estilo personalizado para el encabezado del mes -->
    <style name="CalendarHeaderTextWhite">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">bold</item>
    </style>

</resources>